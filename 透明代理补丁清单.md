# OpenResty TPROXY / 透明代理补丁清单

| 项目 | 说明 |
|------|------|
| **文档日期** | 2026-05-29 |
| **适用版本** | OpenResty 1.25.x（麒麟 V10 / Linux 4.19+） |
| **数据面对齐** | `lualib/gateway/stream/access.lua`、`store/tcp_route.lua`、`conf/nginx.conf` |
| **Lab 现状** | 显式 `:9000` / `nc :12345` ✅；nat **DNAT** ✅；mangle **TPROXY** ❌（`lo:12345` 无包） |

本文是 **OpenResty/nginx 源码侧** 需补全或验证的能力清单，不限于 `$proxy_destination_*`。运维 iptables 见 [透明代理 TPROXY 部署指南](../deployment/20260525_透明代理TPROXY部署指南.md)。

---

## 1. 优先级总览

| 级别 | 能力 | 不做的后果 | Lab 替代 |
|------|------|------------|----------|
| **P0** | TPROXY 连接真正 `accept`（§2） | iptables 计数涨、客户端超时、无 Stream 日志 | nat DNAT |
| **P0** | 读 orig_dst 并暴露变量/Lua API（§3） | 无法 `match.destination_port` / 多应用分流 | Etcd 单路由、不配 destination_port |
| **P1** | 区分 TPROXY / 显式连接（§4） | 误嗅探、路由逻辑混乱 | 9000 与 12345 分口 |
| **P1** | L7 升格 + PROXY 内环（§5） | 透明访问 `IP:443` 无法走 HTTP 路由 | 域名走 443，不走 TPROXY |
| **P2** | upstream `proxy_bind transparent`（§6） | 后端看不到真实客户端 IP | 网关 IP 连 upstream |
| **P2** | IPv6 / nftables 等价（§7） | 双栈环境不可用 | 仅 IPv4 Lab |
| **P3** | 审计字段、测试套件（§8–§9） | 排障与回归成本高 | 手工 tcpdump |

---

## 2. P0 — Stream 透明监听与 TPROXY accept（当前最大缺口）

> **你已做**：`listen 12345 transparent` 配置语法可解析、`ss` 可见监听。  
> **Lab 证明未完成**：mangle TPROXY 后 `tcpdump -i lo port 12345` **无包**；`nc 172.20.20.156:12345` **有** preread。

### 2.1 监听 socket 创建（`ngx_stream_core_module` / `ngx_event_listen`）

| # | 补丁项 | 实现要点 | 验收 |
|---|--------|----------|------|
| 2.1.1 | 解析 `transparent` 标志 | `listen` 指令增加 `transparent`/`TRANSPARENT` | `nginx -t` 通过 |
| 2.1.2 | `IP_TRANSPARENT` | `setsockopt(fd, SOL_IP, IP_TRANSPARENT, 1)` 在 **listen fd** | strace 可见 |
| 2.1.3 | `IP_RECVORIGDSTADDR` | `setsockopt(fd, SOL_IP, IP_RECVORIGDSTADDR, 1)` 在 listen fd（IPv4） | 与 2.1.2 同路径 |
| 2.1.4 | IPv6 等价 | `IPV6_TRANSPARENT`、`IPV6_RECVORIGDSTADDR`（若产品要 v6） | 可选 P2 |
| 2.1.5 | 与 `reuseport` 组合 | 每个 worker 独立 listen fd，均设上述选项 | 多 worker 下 TPROXY 均可用 |
| 2.1.6 | 与 `ssl`/`ssl_preread` 组合 | **本网关**：TPROXY 口为明文 MySQL/SSH；TLS 嗅探在 Lua peek 后升格，**不在** TPROXY server 开 `ssl_preread` | 与 `access.lua` 一致 |

### 2.2 accept 路径（关键：为何 lo 无包）

TPROXY 重定向的连接，**四层目的地址仍是原始应用 IP:port**（如 `101.33.226.19:3306`），不是 `网关:12345`。

| # | 补丁项 | 实现要点 | 验收 |
|---|--------|----------|------|
| 2.2.1 | 接受「非本机目的 IP」的 SYN | transparent listen 必须走 **IP_TRANSPARENT** 语义；仅改配置字符串不够 | TPROXY 后 `lo` 或 `any:12345` 可见 SYN/SYN-ACK |
| 2.2.2 | 与 `ip rule fwmark + table local` 配合 | 内核把 marked 包送到 local 路由；nginx 必须在 **12345** 上 transparent accept | 客户端收到 SYN-ACK |
| 2.2.3 | `route_localnet`（可选） | 部分内核/发行版需 `net.ipv4.conf.all.route_localnet=1` + `TPROXY --on-ip 127.0.0.1` | 文档化，非 nginx 补丁 |
| 2.2.4 | 连接建立日志 | accept 成功时 debug 日志：`transparent=1 remote=… local=…` | 排障 |

**参考实现**：对照 HAProxy `source` transparent、Envoy original_dst、或 nginx 社区 TPROXY 补丁（非仅 `listen` 关键字）。

### 2.3 与 DNAT 路径的差异（便于测试）

| 路径 | 四层目的 | nginx 行为 |
|------|----------|------------|
| **DNAT**（Lab 已成功） | `GW_IP:12345` | 普通 accept，**不需要** orig_dst 即可转发 |
| **TPROXY**（目标） | `APP_IP:3306` | 必须 transparent accept + orig_dst |

补丁验收：**先**让 TPROXY 路径达到与 DNAT 相同的「有 preread 日志」，**再**验 orig_dst 数值正确。

---

## 3. P0 — 原始目的地址（orig_dst）

数据面已预留变量名（按优先级 fallback）：

```lua
-- access.lua
ngx.var.proxy_destination_port / proxy_destination_addr
ngx.var.original_dst_port / original_dst_addr
ngx.var.real_server_port / real_server_addr
```

### 3.1 内核读取

| # | 补丁项 | 实现要点 | 验收 |
|---|--------|----------|------|
| 3.1.1 | `getsockopt(IP_ORIGDSTADDR)` | 在 **accept 后**、preread 前，对 **connection fd** 读取 | TPROXY 连接有值 |
| 3.1.2 | 结构体解析 | IPv4：`struct sockaddr_in` → addr + port 主机序 | `101.33.226.19:3306` |
| 3.1.3 | 非 TPROXY 连接 | 显式 `nc GW:12345`、DNAT 连接：变量 **为空** 或仅 DNAT 后目的（文档说明） | `orig_dst=-:- use_dest_match=no` |
| 3.1.4 | 读取失败 | `alert` 日志 + 不断开（或配置 `orig_dst_required on` 时拒绝） | 可观测 |
| 3.1.5 | 每连接缓存 | 存入 `ngx_connection_t` / session，供变量与 Lua 复用 | preread/balancer 一致 |

### 3.2 Nginx 变量

| 变量名 | 作用域 | 建议 |
|--------|--------|------|
| `$proxy_destination_addr` | stream | **推荐主名**，与数据面对齐 |
| `$proxy_destination_port` | stream | 同上 |
| `$original_dst_addr` / `$original_dst_port` | stream | 别名，可选 |

配置示例（补丁 README）：

```nginx
# stream server 内可 log 验证
error_log logs/error.log info;
# 或在 patch 中硬编码 debug 连接日志
```

**验收日志**（与 `access.lua` 一致）：

```text
[Stream] preread ... orig_dst=101.33.226.19:3306 use_dest_match=yes
```

### 3.3 Lua API（推荐，降低耦合）

| # | API | 返回值 | 说明 |
|---|-----|--------|------|
| 3.3.1 | `ngx.req.orig_dst()` | `{ addr = "101.33.226.19", port = 3306 }` 或 `nil` | stream preread/content 可用 |
| 3.3.2 | `ngx.req.orig_dst("addr")` / `("port")` | 标量 | 可选 |
| 3.3.3 | lua-resty-core 导出 | `resty.core.request` 或独立 `resty.tproxy` | 便于单元测试 mock |

数据面后续可改为优先 `ngx.req.orig_dst()`，变量作 fallback。

### 3.4 与 Etcd 路由匹配

| 字段 | 条件 |
|------|------|
| `listen.port` | 固定 `12345`（`GATEWAY_TPROXY_STREAM_PORT`） |
| `match.destination_port` | **仅** orig_dst 存在时参与（`tcp_route.lua` 已实现） |
| `match.destination_cidr` | **待实现**：需 `$proxy_destination_addr` + Lua CIDR 匹配 |
| `match.sni` | TLS 升格或独立 ssl_preread server；纯 TCP 跳过 |

---

## 4. P1 — 连接类型识别与阶段行为

| # | 补丁/配置项 | 说明 |
|---|-------------|------|
| 4.1 | TPROXY vs 显式 | 建议以 **orig_dst 是否存在** 为准（比「是否 transparent listen」更准，DNAT 到 12345 无 orig_dst） |
| 4.2 | `preread_by_lua` 触发 | 明文 TCP **不要** 在 TPROXY server 开 `ssl_preread`（MySQL 已验证） |
| 4.3 | `ngx.req.socket():peek()` | 仅 TPROXY + orig_dst 为 80/443 等时 peek（`access.lua` `_TPROXY_PEEK_PORTS`）；避免 MySQL 死锁 |
| 4.4 | 同端口冲突 | **禁止** 同一端口同时 `listen 12345` 与 `listen 12345 transparent`（nginx 冲突） |
| 4.5 | worker 间 session | `ngx.ctx` 仅在单连接内；orig_dst 写入 connection 级存储供 balancer 使用 |

---

## 5. P1 — L7 升格（stream → http 内环）

设计：TPROXY 嗅探到 HTTP/TLS 后，转发到 `127.0.0.1:14443`（或 `14480` 明文），由 **http{}** 做域名路由 / 跨区。

### 5.1 Stream 侧（补丁或 Lua + 现有模块）

| # | 项 | 说明 |
|---|-----|------|
| 5.1.1 | 首包嗅探 | 已实现于 `access.lua`（HTTP 方法 / TLS 0x16） |
| 5.1.2 | 向内环建连 | `stream_proxy_target = 127.0.0.1:14443` |
| 5.1.3 | **PROXY protocol v2 发送** | stream 作为 client 连内环时，首包发送 PROXY v2，携带：**src**=客户端 IP:port，**dst**=orig_dst（应用 IP:port） |
| 5.1.4 | TLV 扩展（可选） | `route_id`、`tcp_route_id` 自定义 TLV，供 http access 审计 |

若不在 nginx 内核发 PROXY，可用 **lua-resty-http** / cosocket 手写 PROXY 头（性能与维护性较差，补丁更优）。

### 5.2 HTTP 侧（配置 + 小补丁）

| # | 项 | 说明 |
|---|-----|------|
| 5.2.1 | `listen 127.0.0.1:14443 ssl proxy_protocol` | 内环 server |
| 5.2.2 | `listen 127.0.0.1:14480 proxy_protocol` | 明文 HTTP 内环（若与 14443 分离） |
| 5.2.3 | `$proxy_protocol_addr` / `$proxy_protocol_server_addr` | 恢复客户端 IP、**原始目的 IP**（v2 dst） |
| 5.2.4 | `real_ip` / access.lua | 用 PROXY dst 构造 `Host`/路由键，而非 `127.0.0.1` |
| 5.2.5 | SNI 传递 | TLS 升格时：stream peek 得到 SNI → 写 PROXY TLV 或 `ssl_session` 共享 |

### 5.3 独立 ssl_preread server（可选架构）

若要在 **未 peek** 前读 TLS SNI 选路由：

```nginx
# 仅 TPROXY + 443 场景，与 MySQL server 分离
server {
    listen 12345 transparent;
    ssl_preread on;
    preread_by_lua { ... }
}
```

MySQL/SSH 走 **无** ssl_preread 的 server（需 **不同 listen 端口** 或按 orig_dst 端口在 iptables 分流到不同 TPROXY 口）。单口方案依赖 orig_dst + Lua，不用 ssl_preread。

---

## 6. P2 — 上游透明回连（客户端 IP 保留）

| # | 项 | 说明 | 依赖 |
|---|-----|------|------|
| 6.1 | `ip_nonlocal_bind on` | stream/http 块 | nginx **官方指令**（1.9.1+）；你环境曾报 unknown，查编译 `--with-stream` 与模块列表 |
| 6.2 | `proxy_bind $remote_addr transparent` | stream `proxy_pass` / upstream | `IP_TRANSPARENT` on **outbound** socket |
| 6.3 | `balancer_by_lua` 透传 | `ngx_balancer.set_current_peer(host, port, { bind = true, transparent = true })` 或等价 opts | OpenResty balancer API 扩展 |
| 6.4 | 回程路由 | 客户端到网关、网关以客户端 IP 访问后端时，**回程**须可达 | 运维 `ip rule`，非 nginx |
| 6.5 | 与 DNAT Lab 关系 | DNAT 场景 upstream 源 IP 为 **网关 IP**，一般 **不需要** 6.1–6.4 | 当前 MySQL Lab |

---

## 7. P2 — IPv6 / nftables / 其他平台

| # | 项 | 说明 |
|---|-----|------|
| 7.1 | IPv6 orig_dst | `IPV6_ORIGDSTADDR`、`sockaddr_in6` |
| 7.2 | iptables-nft | 与 legacy 规则等价；`TPROXY` target 行为一致 |
| 7.3 | Kylin 4.19 | 确认 `CONFIG_NETFILTER_TPROXY`、`CONFIG_IP_NF_TARGET_TPROXY`；`xt_TPROXY` 模块 |
| 7.4 | `SO_MARK` | 可选：连接 mark 与 fwmark 区分，便于 tcpdump |

---

## 8. P3 — 可观测性与错误处理

| # | 项 | 说明 |
|---|-----|------|
| 8.1 | 连接级 debug | `transparent=1 orig_dst=… remote=…`（可 `debug_connection` 或编译开关） |
| 8.2 | orig_dst 失败 | `alert` + 计数器（stub_status 或 prometheus，若有） |
| 8.3 | 与 Stream 日志对齐 | 字段名：`orig_dst_addr`、`orig_dst_port`、`use_dest_match`、`stream_mode` |
| 8.4 | 审计 | 日志 JSON 增加 `tcp_route_id`、`orig_dst`，供 ELK |

---

## 9. P3 — 测试与交付物

### 9.1 单元 / 集成测试（nginx `prove` / `Test::Nginx`）

| 用例 | 步骤 | 期望 |
|------|------|------|
| T1 显式 connect | `nc 127.0.0.1:12345` | accept 成功，orig_dst 空 |
| T2 模拟 TPROXY | 用户态工具或 `iptables TPROXY` 打本机 | lo:12345 有流量，orig_dst 正确 |
| T3 MySQL 透传 | TPROXY → stream → backend mock | 无 peek 阻塞 |
| T4 多 destination_port | 两条路由 3306/22 | 按 orig_dst 端口命中 |
| T5 L7 TLS | TPROXY 443 → 内环 14443 | PROXY v2 dst=orig |

### 9.2 补丁交付清单（PR / 内部仓库）

- [ ] 修改文件列表（`ngx_stream_core_module.c`、`ngx_event.c` 等）
- [ ] `README-TPROXY.md`：变量名、sysctl、iptables 最小示例
- [ ] 与 **stock nginx 1.25.x** diff，便于升级 rebase
- [ ] `nginx -V` 输出增加 `--with-stream-tproxy` 或等价 `--add-module` 标记
- [ ] 回归：普通 stream `listen 9000` 不受影响

### 9.3 与数据面联调验收（你环境的 Checklist）

```bash
# 1. TPROXY 投递
sudo tcpdump -i lo -n 'tcp port 12345' -c 5   # 必须有包

# 2. orig_dst
grep 'use_dest_match=yes' logs/error.log

# 3. 多应用
# Etcd 两条：destination_port 3306 / 22，分别 mysql / ssh

# 4. 显式 9000 仍可用
mysql -h GW -P9000 ...

# 5. 关 DNAT，仅 mangle TPROXY
```

---

## 10. 建议修改的 nginx 源码位置（指引）

> 行号随版本变化，仅作搜索入口。

| 区域 | 典型文件 | 改动 |
|------|----------|------|
| listen 解析 | `src/stream/ngx_stream_core_module.c` | `transparent` flag |
| listen socket | `src/event/ngx_event.c` | `IP_TRANSPARENT`、`IP_RECVORIGDSTADDR` |
| accept | `src/event/ngx_event_accept.c` | transparent accept 条件 |
| orig_dst | 新建或 `ngx_event_accept.c` / connection struct | `getsockopt(IP_ORIGDSTADDR)` |
| 变量 | `src/stream/ngx_stream_variables.c` | `$proxy_destination_*` |
| upstream bind | `src/stream/ngx_stream_proxy_module.c` | `proxy_bind … transparent` |
| HTTP PROXY | `src/http/ngx_http_proxy_module.c` | 内环 `proxy_protocol`（多为配置） |
| Lua | `ngx_lua_module` / `ngx_stream_lua_module` | `ngx.req.orig_dst()` |

可参考上游：**nginx-original-dst** 类模块、OpenResty 历史 issue、HAProxy `bind transparent`。

---

## 11. 当前环境对照表

| 能力 | 状态 | 证据 |
|------|------|------|
| `listen transparent` 语法 | ⚠️ 部分 | `nginx -t` 过，`ss` 有 12345 |
| TPROXY accept | ❌ | lo 无 12345；仅 SYN 重传 |
| orig_dst 变量 | ❌ | `orig_dst=-:- use_dest_match=no`（显式 nc） |
| stream preread + 路由 + balancer | ✅ | nc 日志完整 |
| nat DNAT Lab | ✅ | mysql 成功 |
| `ip_nonlocal_bind` | ❓ | 曾报 unknown directive，需查编译选项 |
| L7 升格 + PROXY 内环 | ⏸ | 依赖 P0 orig_dst + 内环 server |

---


## 修改结果

主要内容：

- ngx_connection_t 新增连接级 proxy_original_sockaddr/proxy_original_socklen，不再覆盖 c->local_sockaddr。
- accept 后在 transparent IPv4 连接上读取 SO_ORIGINAL_DST，成功后缓存到连接对象。
- stream 新增变量：$proxy_destination_addr / $proxy_destination_port，并加了兼容别名 $original_dst_*、$real_server_*。
- 清理了 ###T_PROXY_ACCEPT_HIT### 的 NGX_LOG_EMERG 临时日志。
- 修掉了 ngx_conf_log_error(...); 占位代码。
- 去掉了 ngx_config.h 里强行定义 NGX_HAVE_TRANSPARENT_PROXY 1，回到 auto feature 探测。
- 补了 listen fd 上 IPv6 IPV6_TRANSPARENT 的 guarded 设置。
- 清理 HTTP/Stream server selection 里上一轮因覆盖 local_sockaddr 引入的异常逻辑。

验证情况：

- 已跑 git diff --check -- <本次触碰的源码文件>，通过。



